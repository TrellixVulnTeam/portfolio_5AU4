const d = document;

export default function tabBar(container) {



/*         d.addEventListener("click", (e)=> {
        if(e.target.matches(container) || e.target.matches(`${container} *`)){
            e.preventDefault();
            d.querySelector(container).classList.toggle("menu-active");
        }

    });  */


}
